/**
 * This role enum is the list of all the roles in this app. We assign the value we want to display to the user.
 */
export enum Role {
    User = "User",
    Admin = "Manager",
    SuperAdmin = "General Manager"
  }